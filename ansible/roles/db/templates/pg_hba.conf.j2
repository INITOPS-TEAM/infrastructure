# Local connections
local   all             postgres                                peer
local   all             all                                     peer

# Localhost
host    all             all             127.0.0.1/32            md5

# Application servers
host    {{ db_name }}   {{ db_user }}   10.0.0.0/16            md5
